1、扫描机制重整；over
2、上下文外拦截器、上下文内拦截器，多层wsgi_app函数替换与包裹 over
3、配置拦截器初始化函数、wsgi_app执行顺序、wsgi公用量研究
4、异常捕获体系
5、run方法用类定义时执行函数替换，不定义函数，socketio时直接柯里化替换app.run
6、视图类、视图函数(视图需要继承，但千万记住，视图不用扫描，不然就循环导入了)(后续需要将DRF的视图集概念搬过来)
7、序列化器（需要为序列化器的字段验证、校验等编写实现）
8、路由重整(over)
9、flask_sqlalchemy内置(定义了主次连接信息再执行，否则光走这里不执行的)